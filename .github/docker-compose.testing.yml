version: '3.8'

services:
  app-name:
    volumes:
      - ..:/workspace
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile        # * should be the same container as used in devcontainer environment
    tty: true                                     # * will keep container running so we can execute commands on it
    container_name: app-name-pipeline-container
